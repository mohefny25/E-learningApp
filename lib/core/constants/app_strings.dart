/// Non-user-facing string constants (brand name, and any future internal
/// identifiers/keys). User-facing text lives in the ARB localization files.
class AppStrings {
  /// Official brand name. Never translated and never placed in ARB files.
  static const String appName = 'YOUR ACADEMY';
}
